import { ImageSearchboxItem } from './image-searchbox-item';

describe('ImageSearchboxItem', () => {
  it('should create an instance', () => {
    expect(new ImageSearchboxItem()).toBeTruthy();
  });
});
